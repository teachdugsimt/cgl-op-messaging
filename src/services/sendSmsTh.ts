// const axios = require('axios');

export const thsmsSender = async (message, phoneNumber) => {
    // const result = await axios.post('http://www.thsms.com/api/rest?method=send&username=cargolink&password=35c0c5&from=0000&to=' + phoneNumber + '&message=' + encodeURI(message))
    // return result.status == 200
    return false
}
